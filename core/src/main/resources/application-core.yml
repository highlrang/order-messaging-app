spring:
    datasource:
        url: jdbc:mysql://host.docker.internal:3306/commercedb?useSSL=false&allowPublicKeyRetrieval=true
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        password: 
    
    jpa:
        hibernate:
            ddl-auto: create
            

logging:
    level:
        root: DEBUG
        org:
            hibernate:
                type: TRACE
                sql: DEBUG

header:
    secretKey: gpejrhdxhddlswmdzl123!@#